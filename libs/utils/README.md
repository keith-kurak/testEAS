# utils

This library was generated with [Nx](https://nx.dev).

It contains generic utility functions shared across apps and libs:

- common utils
- device related utils
- files related utils
- number related utils
- string related utils

## Running unit tests

Run `nx test utils` to execute the unit tests via [Jest](https://jestjs.io).
